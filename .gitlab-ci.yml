pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp paper/Machine_Economy_Lab_Research_Paper.html public/index.html
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
