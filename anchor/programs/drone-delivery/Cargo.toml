[package]
name = "drone-delivery"
version = "0.1.0"
description = "DePIN Drone Proof-of-Delivery — Phase 1"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "drone_delivery"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
